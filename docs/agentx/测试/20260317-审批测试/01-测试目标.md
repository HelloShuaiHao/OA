# 测试目标

## 验证内容

1. **OpenFang Agent 能否通过 web_fetch 调用 OA 接口**
2. **两个场景的 Agent 是否能正确工作**
   - 请假申请助手
   - 审批助手
3. **多租户隔离是否有效**

## 成功标准

- Agent 能理解自然语言并调用正确的接口
- 接口调用成功，返回正确结果
- 多个租户的 Agent 实例互不干扰
